@model IEnumerable<Ekip>
@{
    ViewData["Title"] = "Ekipler";
}
<style>
    .Asagıyacek {
        max-height: 620px;
    }

    .card-body {
        margin-bottom: 30px;
    }
</style>
<div class="container mt-4">
    @{
        var message = TempData["Message"] as string;
        if (!string.IsNullOrEmpty(message))
        {
            <div class="alert alert-danger">
                @message
            </div>
        }
    }
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Ekip Listesi</h2>
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> Yeni Ekip
        </a>
    </div>

    <div class="card">
        <div class="card-body">
            <div class="table-responsive Asagıyacek">
                <table class="table table-striped datatable">
                    <thead>
                        <tr>
                            <th>Ekip Adı</th>
                            <th>Şirketler</th>
                            <th>Durum</th>
                            <th>İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.EkipAdi</td>
                                <td>
                                     
                                     @string.Join(", ", item.SirketEkips?.Select(se => se.Sirket?.SirketAdi) ?? Array.Empty<string>()) 
                                </td>
                                <td>@(item.AktifMi == true ? "Aktif" : "Pasif")</td>
                                <td>
                                    <div class="btn-group btn-group-sm">
                                        <a asp-action="Edit" asp-route-id="@item.EkipId" class="btn btn-warning">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.EkipId" class="btn btn-danger">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
